"""Lectura del manual de Intel y composicion del formato `isadoc`.

El paquete esta partido por responsabilidad y no por comodidad: el volcado de
un PDF falla de formas muy distintas -- una columna que se corre, una tabla que
se traga el parrafo siguiente, un titulo que comparte linea con el pie -- y
cada una necesita su propia comprobacion.

Como se usa, que hace cada modulo y por que existe cada verificacion esta en
`tools/README.md`.
"""
